# Archive

Internal build journal — kept for the curious, not needed to run LAX.

- `adr/` — 25 Architecture Decision Records (Phase 0 → V2): why OpenCode + NIM,
  why Turnkey-custodied wallet, why HF 1.05 proactive trigger, why the fork demo, …
- `phase0/` … `phase4/` — per-phase research and plans from the original build
  (platform analysis, liquidation mechanics research, dashboard template research)
- `cli_phase/` — the V2 Liquidation CLI build plan and architecture drafts
- `MISSION.md`, `SCOPE` history, `RISK_REGISTER.md`, `P3-BOUNTY-PLAN.md`,
  `UX-FIX-PROMPT.md` — planning artifacts
- `bounty-pr-gas-sponsorship-fallback.{md,patch}` — the Bounty PR #2268
  (gas-sponsorship fallback docs) patch and description; the PR itself is merged
- `research/` — prompt-engineering notes from the build process

Current, curated documentation lives one level up — start at [`docs/README.md`](../README.md).
